<template>
	<div class="ts-grid is-stacked is-compact">
		<div class="column">
			<span class="problem-score">(10%)</span>
			_______ <vl exp="R" /> is a non-symmetric relation on <vl exp="A" />
			if there exist <vl exp="x, y \in A" /> such that <vl exp="(x, y) \in R" />
			but <vl exp="(y, x) \notin R" />. If <vl exp="|A| = m" />,
			how many non-symmetric relations on <vl exp="A" /> are there?
		</div>
		<div class="column ts-wrap">
			<span>(A) <vl exp="2^{(m^2-m)/2}" /></span>
			<span>(B) <vl exp="2^{m^2} - 2^{(m^2+m)/2}" /></span>
			<span>(C) <vl exp="3^{(m^2-m)/2}" /></span>
			<span>(D) <vl exp="2^{(m^2+m)/2}" /></span>
			<span>(E) <vl exp="2^{m^2-m}" /></span>
		</div>
	</div>
</template>
