<template>
	<MakeProblem scoreText="(15%)">
		<template #problem>
			Let $N$ be the set of all positive integers, and $S = N \times N$, (namely, $S = \{(x, y) \mid x, y \in N\}$).
			Show that there is a bijection (one‑one and onto) function $f: S \to N$, or show that no such function exists.
		</template>
	</MakeProblem>
</template>
