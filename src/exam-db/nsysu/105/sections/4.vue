<template>
	<MakeProblem scoreText="(15%)">
		<template #problem>
			A line segment in $\mathbb{R}^2$ is a line joining 2 different points in $\mathbb{R}^2$.
			Two lines intersect if they share a common point. Show that in any 6 line segments in $\mathbb{R}^2$,
			either there are 3 line segments in which any pair of them intersect or there are 3 line segments
			in which no pair of them intersect.
		</template>
	</MakeProblem>
</template>
