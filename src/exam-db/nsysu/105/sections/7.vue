<template>
	<MakeProblem scoreText="(15%)">
		<template #problem>
			Let $L$ be the set of binary strings whose values are multiple of 7,
			(namely, $L = \{0,111,1110,10101,\cdots\}$). Design a finite state machine $M = (Q,\Sigma,\delta,q_0,F)$ to recognize the language $L$. Although no formal proof is required, you should explain why your machine $M$ accepts only strings in $L$, and every binary string in $L$ is accepted by $M$.
		</template>
	</MakeProblem>
</template>
