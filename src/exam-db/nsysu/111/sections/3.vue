<template>
	<MakeProblem scoreText="[10%]">
		<template #problem>
			Let $n \in \mathbb{Z}^+$ with $n \gt 1$, and let $A$ be the set of positive integer divisors of $n$.
			Define the relation $R$ on $A$ by $xRy$ if $x$ (exactly) divides $y$.
			Determine how many ordered pairs are in the relation $R$ when $n$ is $6680$.
		</template>
	</MakeProblem>
</template>
