<template>
	<MakeProblem scoreText="(3 points)">
		<template #problem>
			Let $v_1 = (2,6)^T,~ v_2 = (1,4)^T$, and $S = \!\begin{bmatrix} 4 & 1 \\ 2 & 1 \end{bmatrix}\!$.
			Find vectors $u_1$ and $u_2$ so that $S$ will be the transition matrix from
			the ordered basis $\{ u_1, u_2 \}$ to the ordered basis $\{ v_1, v_2 \}$.
		</template>
	</MakeProblem>
</template>
