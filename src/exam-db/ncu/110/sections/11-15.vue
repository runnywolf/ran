<template>
	<MakeProblem scoreText="">
		<template #problem>
			the necessary conditions for matrix $A$ can be factorized （複選）
			11. $A = PDP^{-1}$.
			12. $A = QR$.
			13. $A = PDP^T$.
			14. $A = \lambda_1 u_1 u_1 T + \lambda_2 u_2 u_2 T + ... + \lambda_n u_n u_n T$.
			15. $A = R^T R$ (Cholesky factorization)
			(A) square but non-symmetric, (B) square and symmetric, (C) linearly-independent columns,
			(D) linearly-independent eigenvectors, (E) positive definite
		</template>
	</MakeProblem>
</template>
