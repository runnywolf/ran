<template>
	<MakeProblem scoreText="">
		<template #problem>
			"For a $n \times n$ real square matrix $A$. $\lambda_i$'s are eigenvalues of $A$.
			(A) $\lambda_i$'s may be complex number.
			(B) If some $\lambda_i = 0$, then $A$ is always not invertible.
			(C) If some $\lambda_i$'s are the same, then $A$ is not diagonalizable.
			(D) If $A = A^T$, then $\lambda_i$'s are positive real.
			(E) If $A$ is diagonalizable, then $A$ has many different matrices $P$ and $D$ such that $A = PDP^{-1}$."
		</template>
	</MakeProblem>
</template>
