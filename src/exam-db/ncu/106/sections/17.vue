<template>
	<MakeProblem listEndLabel="e">
		<template #problem>If $A$ is a $n \times n$ matrix with real entries, then</template>
		<template #a>$A$ has $n$ eigenvalues exactly.</template>
		<template #b>$A$ has $n$ eigenvectors exactly.</template>
		<template #c>$A$ has $n$ eigenspaces exactly.</template>
		<template #d>The bases of all $A$’s eigenspaces are not always linearly independent.</template>
		<template #e>If $A$ has complex eigenvalues, then $A$ must has complex eigenvectors.</template>
	</MakeProblem>
</template>
