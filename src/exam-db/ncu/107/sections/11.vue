<template>
	<MakeProblem listEndLabel="E">
		<template #problem>Concept of linear independent.</template>
		<template #A>
			The linear system $Ax = b$ has unique solution for $b$, then the columns of $A$ are linearly independent.
		</template>
		<template #B>The columns of the change‑of‑coordinate matrix $P$ are linearly independent.</template>
		<template #C>The subset of a linearly‑dependent vector set is linearly dependent.</template>
		<template #D>If $A$ is diagonalization, then $A$ has linearly‑independent columns.</template>
		<template #E>If $A^T A$ is invertible, then $A$ has linearly‑independent columns.</template>
	</MakeProblem>
</template>
