<template>
	<MakeProblem listEndLabel="E">
		<template #problem>
			9. Let $f: A \rightarrow B$ and $g:B \rightarrow C$ be functions. What following statements are true?
			A. if $g \circ f$ is one-to-one, so is $f$.
			B. if $g \circ f$ is one-to-one, so is $g$.
			C. if $g \circ f$ is onto, so is $f$.
			D. if $g \circ f$ is onto, so is $g$.
			E. if $g \circ f$ is a bijection, so are $g$ and $f$.
		</template>
		<template #A></template>
		<template #B></template>
		<template #C></template>
		<template #D></template>
		<template #E></template>
	</MakeProblem>
</template>
