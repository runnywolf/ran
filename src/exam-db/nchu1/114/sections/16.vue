<template>
	<MakeProblem listEndLabel="D">
		<template #problem>
			Which of the following statement is <span class="ran-bold">False</span>?
		</template>
		<template #A>
			The orthogonal projection of a vector on a line is a vector that lies along the line.
		</template>
		<template #B>
			If the norm of $v$ equals 0, then $v=0$.
		</template>
		<template #C>
			For all vectors $u$ and $v$ in $\mathbb{R}^n$, $|u \cdot v| = \lVert u \rVert \cdot \lVert v \rVert$.
		</template>
		<template #D>
			If $w$ is the orthogonal projection of $u$ on a line through the origin of $\mathbb{R}^2$,
			then $u-w$ is orthogonal to every vector on the line.
		</template>
	</MakeProblem>
</template>
